type Personalities @model @auth(rules: [{allow: owner}, {allow: public, provider: iam, operations: [read]}]) {
  id: ID!
  name_1: String
  personality_1: String
  name_2: String
  personality_2: String
  user_email: String
}

type Chat @model @auth(rules: [{allow: owner}, {allow: private}, {allow: public, provider: iam, operations: [create, read]}]) {
  id: ID!
  message: String!
  message_in_thread: Int
  user_email: String
  speaker_name: String
  thread_id: ID
}

type Bot @model @auth(rules: [{allow: owner}, {allow: public, provider: iam}]) {
  id: ID!
  bot_order: Int!
  bot_name: String
  bot_personality: String
} 