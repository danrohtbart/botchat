type Headline @model @auth(rules: [{allow: public}]) {
  id: ID!
  message: String
}

type Bot @model @auth(rules: [{allow: public}, {allow: owner}]) {
  id: ID!
  bot_name: String
  bot_personality: String
  bot_url: AWSURL
}
